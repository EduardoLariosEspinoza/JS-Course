const header = document.getElementsByClassName('header');
console.log(header);

const hero = document.getElementsByClassName('hero');
console.log(hero);

const contenedores = document.getElementsByClassName('contenedor'); // Todos los elementos que tengan en el el nombre de su clase 'contenedor'
console.log(contenedores); // Coleccion con todos los elementos

const noExiste = document.getElementsByClassName('noExiste'); // Coleccion vacia
console.log(noExiste);